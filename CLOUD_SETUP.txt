CLOUD KURULUM — ChronoShorts FULL AUTOPILOT (PC KAPALI)
======================================================
Bilgisayar KAPALI olabilir. Günde 3 Short üretilir ve insan onayı /
Approve kuyruğu BEKLENMEDEN YouTube + Instagram + TikTok'a (secret varsa)
PUBLIC olarak yüklenir. schedule.require_approval=false | auto_upload=true.

Görseller (ücretli API — ~$30/ay bütçe):
  media.provider=ai_api · backend=fal_flux_schnell (fal.ai Flux Schnell)
  Hedef hacim: ~3 Short/gün × ~11 görsel ≈ 900–1100 görsel/ay
  Flux Schnell ≈ $0.003/MP → ~$0.003–0.006/görsel → ayda ~$6–10 (cap $30)
  Secret: FAL_KEY (veya IMAGE_API_KEY). Yoksa Pollinations ($0) fallback.
  Bütçe log: logs/budget.json + panel /status

İçerik stratejisi (önemli):
  Hedef = Amerikan izleyici dikkati (USA-only değil).
  Konu havuzu: ABD tarihi + dünya tarihi (Roma, WWII, Soğuk Savaş…) +
    ünlü figürler + "Did you know?" şok fact'ler. Hepsi İngilizce.
  Senaryolar: scenarios/history_hooks.json | Kuyruk: topics/queue.json (~30)
  Stil: prompts/image_style.txt · TTS: Kokoro am_adam

Saatler (Türkiye / Europe/Istanbul = GMT+3):
  00:00  →  UTC 21:00 (bir gün önce)
  08:00  →  UTC 05:00
  16:00  →  UTC 13:00

Akış (FULL AUTOPILOT — insan onayı YOK):
  GitHub Actions tetiklenir → Docker içinde video render (Kokoro/Whisper/FFmpeg)
  → fal.ai / Pollinations AI stills → SEO → HEMEN yükle (Approve beklenmez)
  → YouTube Shorts (public) + Instagram Reels + TikTok (secret varsa)
  → logs/latest.json + jobs.jsonl + budget.json + schedule.log commit
  config: schedule.auto_upload=true, schedule.require_approval=false

  Atelier dashboard /status = kontrol paneli (schedule, jobs, budget, platforms).
  Approve kuyruğu SADECE elle/yerel denemeler içindir; bulut schedule beklemez.

ÖNEMLİ: Bilgisayarın KAPALI olabilir. Render bulutta olur.
Site deploy: DEPLOY.md (Vercel → apps/web → /status)


1) GitHub reposu oluştur ve kodu yükle
--------------------------------------
PowerShell (proje kökünden):

  cd "C:\Users\Ömür KIRBIYIK\Desktop\yt otomatizasyon"
  git init
  git add .
  git status
  # credentials/ ASLA eklenmemeli (.gitignore'da)
  git commit -m "ChronoShorts cloud schedule"
  git remote add origin https://github.com/ArdaYK31/shorts-oto.git
  git branch -M main
  git push -u origin main

Repo Settings → Actions → General → "Read and write permissions" aç.


2) GitHub Secrets ekle (Settings → Secrets and variables → Actions)
-------------------------------------------------------------------
A) YouTube (zorunlu — yoksa YT atlanır)

  YOUTUBE_CLIENT_SECRET_JSON  → credentials\client_secret.json içeriği
  YOUTUBE_TOKEN_JSON          → credentials\youtube_token.json içeriği
  Privacy: PUBLIC

B) fal.ai görseller (önerilen — ~$30/ay bütçe)

  FAL_KEY
    → https://fal.ai → Dashboard → API Keys → Create key
    → GitHub → Settings → Secrets → Actions → New → Name: FAL_KEY
  IMAGE_API_KEY (opsiyonel alias)

  CLI (gh kuruluysa):
    gh secret set FAL_KEY --body "$env:FAL_KEY" --repo ArdaYK31/shorts-oto

  FAL_KEY yoksa Pollinations ücretsiz fallback.
  Cap: config.yaml media.ai_api.budget.monthly_cap_usd: 30

C) Instagram (opsiyonel)
  META_ACCESS_TOKEN, IG_USER_ID, META_GRAPH_VERSION

D) TikTok (opsiyonel)
  TIKTOK_ACCESS_TOKEN, TIKTOK_MODE


3) Actions + Atelier
--------------------
  Actions → ChronoShorts Schedule → Enable → dry_run testi
  Site: DEPLOY.md → Vercel root apps/web → /status
  Şifre: AUTH_PASSWORD (varsayılan atelier)


4) Yerel test
-------------
  .\.venv312\Scripts\python.exe src\scheduled_run.py --dry-run
  Log: logs\latest.json · Budget: logs\budget.json


5) Güvenlik
-----------
  credentials/ ASLA commit etme. Secret sızarsa fal/Google/Meta/TikTok'ta iptal et.
