# Python 3.12+ (.venv312). Zero paid APIs. English video content only.
PyYAML>=6.0
requests>=2.31.0
Pillow>=10.0.0
numpy>=1.26.0
soundfile>=0.12.0
kokoro>=0.9.2
faster-whisper>=1.0.0
tzdata>=2024.1
# Optional local AI stills (ChronoShorts look) — installs separately if you want GPU Diffusers:
#   pip install torch diffusers transformers accelerate --index-url https://download.pytorch.org/whl/cu121
# Without Diffusers, ai_local falls back to free Pollinations HTTP ($0).
# Optional ONNX fallback (models in C:\kokoro-models):
# kokoro-onnx>=0.4.0
# Windows note: copy espeak-ng-data to C:\espeak-ng-data if username has non-ASCII chars.
